# Shared Hairness state

This tracked directory contains distribution-owned policies and explicitly published artifacts.

It never contains machine-specific mounts, runs, transcripts, credentials or scratch. Those remain in `.overlay/`. Personal preferences and trust remain in `~/.hairness/`.

Extensions own every file they publish here. Presence does not activate an extension or grant authority.
