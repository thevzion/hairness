# Changelog

## 0.3.0-alpha.0 — unreleased

Breaking architectural reset:

- replace global protocol/schema versions with type-specific document APIs;
- make `@hairness/cli` the pinned runtime of a lightweight Home;
- reduce Standard to cockpit, work, sources, codebase, and delivery;
- compile exactly ten human commands for Codex and Claude;
- add explicit Scratch memory, typed single-payload Artifacts, local Overlay Git,
  exact Checkpoints, and immutable Receipts;
- add local/path/pinned-Git extension lifecycle with divergence stops;
- add independent Targets, focused maps, typed DeliveryBriefs, named gates, and
  adaptive internal worktrees;
- remove the complete v0.2 orchestration, Forge, material graph, migrations,
  revision graph, tracked provider output, and compatibility surface.

Upgrade by creating a new Home. There is no in-place migration.
