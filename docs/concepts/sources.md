# Sources

Source extensions define typed read operations over tools such as Git, Jira,
GitLab, and AWS. Local CLIs provide strict proof. Provider connectors may assist
a discussion but cannot satisfy a strict source gate on their own.

Source evidence records the operation, transport, observed time, summary,
proof, limits, and freshness information. It never contains credentials.
