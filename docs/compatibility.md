# Compatibility

Hairness 0.4 is a clean reset. It has no v0.3 reader, command alias, migration
engine or automatic Overlay conversion. Create a new Home and copy only human
Scratch material deliberately. Published older packages remain usable when a
project pins them.
