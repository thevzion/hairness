# Provider projections

Provider-specific files are generated from enabled extension manifests. This
directory owns only host syntax, profiles, and hook templates; extensions own
the logical commands.
