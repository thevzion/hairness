Shape a delivery hypothesis before creating durable state. Once the user accepts
it, save one typed DeliveryBrief with outcome, acceptance criteria, scope,
non-goals, Target and base, release impact and required checks. Reuse a compatible
clean checkout or isolate work internally when dirty, occupied, incompatible or
parallel. Stop at named gates and prepare exact effects separately for PR, merge
and release. Never force cleanup or infer authority from repository access.
