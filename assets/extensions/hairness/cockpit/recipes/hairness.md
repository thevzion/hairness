Orient the user without forcing a workflow. Explain only the concepts needed now:
Home contains the harness, Targets are repositories, Scratch is optional explicit
memory, and checkpoints guard effects. If the Home is not onboarded, recommend
`hairness-onboarding`. Otherwise show the smallest useful next command. Explain
the complete human surface when help is requested: orientation and onboarding;
Scratch; discuss, map, ideate, propose, recap and plan; then ship. Keep machine
namespaces out of the tour unless the user asks for deterministic details.
