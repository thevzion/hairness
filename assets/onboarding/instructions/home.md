Hairness is the provider-agnostic place where agents work. The Home owns its
source-owned agentic assets and explicit human memory; Targets remain independent
Git repositories.

Use the session prologue as an orientation index. If it is absent, run the exact
runtime declared by `hairness.json`. Revalidate live evidence before relying on it.

Do not write session transcripts or hidden reasoning. Persist only when the user
explicitly creates or updates a Scratch or accepts a document.
