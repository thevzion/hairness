Hairness is the local agent workspace. The Home owns agentic assets and explicit
human memory; Targets remain independent Git repositories.

Use the session prologue as an orientation index. If it is absent, run
`hairness prologue` once. Revalidate live evidence before relying on it.

Do not write session transcripts or hidden reasoning. Persist only when the user
explicitly creates or updates a Scratch or accepts a document.
