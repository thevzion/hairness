---
name: hairness
description: Route one intention through the active Hairness distribution.
---

Invoke with `/hairness`.

Resolve the intent to one active operation. Use `hairness help --json` only when ownership is unclear, then submit the operation through `hairness invoke start`.

Resolve the human intention, then route it through the smallest relevant Hairness command. Start with help when ownership is unclear.

No authority is implied. Keep checkpoints and worker capsules exact.
