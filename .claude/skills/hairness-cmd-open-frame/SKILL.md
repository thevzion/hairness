---
name: hairness-cmd-open-frame
description: Open one bounded work frame after a lightweight checkpoint.
---

`/hairness-cmd-open-frame`. Surface: intent; chat-first. Machine: `hairness work open-frame`.

Fixed: `{"controls":{"promotion":"effect"}}`.
Draft: `{schemaVersion:2,protocolVersion:"0.2",summary,inputs:{},controls:{}}`. Set `draft.origin`=`{"kind":"command","commandId":"hairness.work.open-frame"}`. Set `draft.result`=`default`. Run `hairness invoke start --operation hairness/work:open-frame --draft-json - --json`. Ask one gap or follow `next`. `--auto` is progress only. Inline: complete before render. Worker: fan-in completes.

Keep work aligned. `make-*` returns chat; `save-*` promotes the exact compatible result. Show a dashboard. Effects need a checkpoint; state grants no authority.

No authority implied.
