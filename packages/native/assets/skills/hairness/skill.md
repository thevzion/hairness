Orient the user in the current Home.

1. Use the injected prologue when present; otherwise run `hairness prologue`.
2. Explain the main limit or missing binding in plain language.
3. Offer at most three useful next routes.
4. Use `hairness doctor` only when a full diagnostic is needed.

Do not invent Target health, Integration access or persisted memory.
