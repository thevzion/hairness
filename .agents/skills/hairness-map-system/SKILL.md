---
name: hairness-map-system
description: Map relationships across the registered codebase system.
---

# hairness-map-system

Invocation: `$hairness-map-system`
Deterministic route: `hairness codebase system --json`
Owner: `hairness/codebase`

Run `hairness codebase system --json`. Use the returned producer capsule and reduce the declared plan before responding.

A command grants no authority. Respect active constraints and checkpoints. A worker receives only its capsule and returns one typed result to the declared fan-in.
