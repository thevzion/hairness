Review the current Hairness change from public contract to evidence.

1. Compare behavior with `README.md`, `docs/reference.md` and the JSON schemas.
2. Check that the Home remains primary, Extensions remain source-owned and Targets remain independent.
3. Reject hidden package-manager behavior, implicit persistence and install-time execution.
4. Run the smallest relevant tests, then the full qualification matrix for a release candidate.
5. Report concrete failures with files and observed commands; do not infer remote health.
