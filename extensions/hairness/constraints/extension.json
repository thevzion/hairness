{
  "$schema": "../../../schemas/extension.schema.json",
  "schemaVersion": 2,
  "protocolVersion": "0.2",
  "id": "hairness/constraints",
  "version": "0.2.0-alpha.0",
  "summary": "Inherited constraints that can tighten but never grant authority.",
  "category": "execution-safety",
  "tags": ["authority", "controls"],
  "maturity": "official",
  "readme": "./README.md",
  "module": "./index.mjs",
  "capabilities": ["./capabilities/constraints.json"],
  "dependencies": ["hairness/work-controls"],
  "contributes": ["authority-policy"],
  "services": ["effective"],
  "commands": [{ "namespace": "constraint", "actions": ["show", "set", "clear"] }],
  "providerCommands": [
    { "id": "hairness.constraint", "name": "hairness-constraint", "summary": "Inspect or change inherited session, segment and frame constraints.", "kind": "capability", "classification": "specialized", "command": "hairness constraint show", "arguments": [{ "name": "action", "required": false }, { "name": "constraint", "required": false }, { "name": "scope", "required": false }], "result": { "schema": "Boundary", "disposition": "run-only" }, "operation": { "capability": "hairness/constraints", "id": "inspect" }, "instructions": "./commands/constraints.md" }
  ]
}
